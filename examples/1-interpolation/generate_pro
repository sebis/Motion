#!/bin/bash

PRO=${PWD##*/}.pro

qmake -project -t app -r -o $PRO \
"LIBS += -L../../engine/lib" \
"LIBS += -lGLEW -lglut -lengine" \
"OBJECTS_DIR = obj"

sed -i '4i include(../../engine/engine.pri)' $PRO

